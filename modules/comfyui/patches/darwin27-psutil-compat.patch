diff --git a/comfy/model_management.py b/comfy/model_management.py
index 9d39be7b..733fe18f 100644
--- a/comfy/model_management.py
+++ b/comfy/model_management.py
@@ -193,7 +193,10 @@ def get_total_memory(dev=None, torch_total_too=False):
         dev = get_torch_device()
 
     if hasattr(dev, 'type') and (dev.type == 'cpu' or dev.type == 'mps'):
-        mem_total = psutil.virtual_memory().total
+        try:
+            mem_total = psutil.virtual_memory().total
+        except Exception:
+            mem_total = 24 * 1024 * 1024 * 1024  # fallback: 24GB (Darwin 27 psutil compat)
         mem_total_torch = mem_total
     else:
         if directml_enabled:
@@ -236,7 +239,10 @@ def mac_version():
         return None
 
 total_vram = get_total_memory(get_torch_device()) / (1024 * 1024)
-total_ram = psutil.virtual_memory().total / (1024 * 1024)
+try:
+    total_ram = psutil.virtual_memory().total / (1024 * 1024)
+except Exception:
+    total_ram = 24 * 1024  # fallback: 24GB in MB (Darwin 27 psutil compat)
 logging.info("Total VRAM {:0.0f} MB, total RAM {:0.0f} MB".format(total_vram, total_ram))
 
 try:
@@ -1292,7 +1298,10 @@ def get_free_memory(dev=None, torch_free_too=False):
         dev = get_torch_device()
 
     if hasattr(dev, 'type') and (dev.type == 'cpu' or dev.type == 'mps'):
-        mem_free_total = psutil.virtual_memory().available
+        try:
+            mem_free_total = psutil.virtual_memory().available
+        except Exception:
+            mem_free_total = 8 * 1024 * 1024 * 1024  # fallback: 8GB free (Darwin 27 psutil compat)
         mem_free_torch = mem_free_total
     else:
         if directml_enabled:
